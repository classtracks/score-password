{
  "name": "score-password",
  "version": "0.8.3",
  "description": "Returns a simple score for a password input",
  "main": "index.js",
  "scripts": {
    "build": "babel source -d build",
    "release": "cp README.md build/ && cp package.json build/ && cd build/ && npm publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": "classtracks/score-password",
  "keywords": [
    "password",
    "score",
    "estimation",
    "strength"
  ],
  "author": "ClassTracks",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/classtracks/score-password/issues"
  },
  "homepage": "https://github.com/classtracks/score-password#readme",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.6.0"
  }
}
